import { createStitches } from "@stitches/react";

export const { config, createTheme, styled, globalCss, getCssText, keyframes, css, theme } = createStitches({
  theme: {
    colors: {
      banana: '#8257e6'
    }
  }
})